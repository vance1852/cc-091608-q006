{
  "name": "pregnancy-bp-triage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc",
    "test": "npm run check && tsc && node --test dist/test/*.test.js",
    "start": "node dist/src/main.js",
    "serve": "node dist/src/serve.js"
  },
  "devDependencies": {
    "@types/node": "latest",
    "typescript": "5.9.2"
  }
}
